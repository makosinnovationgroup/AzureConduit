# Database Configuration
# Supported types: mssql, postgres, mysql
DB_TYPE=postgres

# Database connection settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mydb
DB_USER=myuser
DB_PASSWORD=mypassword

# Optional: Connection pool settings
DB_POOL_MIN=2
DB_POOL_MAX=10

# Server port
PORT=8000
